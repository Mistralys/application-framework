# BigSelection — Checkable items with pre-selected state

Demonstrates a `BigSelectionWidget` where some items are pre-selected on page load.

Items marked with `makeSelected()` have the `active` CSS class on render,
and their hidden input is not disabled, so they submit without any user action.
