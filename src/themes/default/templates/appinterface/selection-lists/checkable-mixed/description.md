# BigSelection — Mixed regular and checkable items

Demonstrates a `BigSelectionWidget` that combines headers, separators,
regular link items, and checkable items in one widget.

Regular items and checkable items coexist. The form name only applies
to checkable items — regular items are unaffected.
