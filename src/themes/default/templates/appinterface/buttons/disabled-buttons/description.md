Buttons can be disabled with `disable()`, regardless of whether they
have a link, a click handler, or no action at all.

An optional reason string can be passed to `disable()` to display a
tooltip explaining why the button is currently unavailable.
