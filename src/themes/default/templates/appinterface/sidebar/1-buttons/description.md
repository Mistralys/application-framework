Sidebar buttons have the same methods as regular UI buttons.
However, they are always block buttons, so they fill the 
available width of the sidebar—which is why they are so
large in this example.
