By default, all data grids will fill the entire available space. 
This can be changed by enabling auto-width mode using the 
`makeAutoWidth()` method. This will make the grid use the width 
of its content.
