The preferred way to add larger blocks of HTML to a section is
to use Output Buffering to capture the content. This keeps the
HTML code readable. Sections have methods specifically for this 
purpose.
