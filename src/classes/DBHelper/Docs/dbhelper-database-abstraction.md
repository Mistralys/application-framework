## Database abstraction

The `DBHelper` class is a static class with methods to access the
application's database, as well as general-purpose utility methods.
It is used to run custom queries against the database.

