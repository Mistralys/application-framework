# Work Packages Draft — Icon Builder CLI: Post-Synthesis Cleanup

> Generated by: Ledger WP Decomposer
> Source plan: `docs/agents/plans/2026-04-20-icon-builder-cleanup/plan.md`

---

## WP-001 — Add `ERROR_END_MARKER_NOT_FOUND` constant and fix end-marker error code

**Description:** Add a dedicated `ERROR_END_MARKER_NOT_FOUND` error constant to `IconBuilder` and use it in the end-marker validation branch of `insertIconCode()`, fixing the bug where both start-marker and end-marker failures reported the same error code. Update the existing test and add a new JS end-marker test.

**Scope:**
- `src/classes/Application/Composer/IconBuilder/IconBuilder.php` — new constant, fix error code in `insertIconCode()`, update `build()` docblock
- `tests/AppFrameworkTests/Composer/IconBuilder/IconBuilderTest.php` — fix existing test assertion, add new test

**Deliverables:**
- New constant `ERROR_END_MARKER_NOT_FOUND = 82305` on `IconBuilder`
- End-marker validation branch in `insertIconCode()` (line ~180) uses `self::ERROR_END_MARKER_NOT_FOUND` instead of `self::ERROR_START_MARKER_NOT_FOUND`
- `build()` method docblock lists `{@see self::ERROR_END_MARKER_NOT_FOUND}` as a separate error case
- Existing test `test_build_phpEndMarkerMissing_returnsError` asserts the new constant
- New test `test_build_jsEndMarkerMissing_returnsError` verifies JS end-marker failure returns `ERROR_END_MARKER_NOT_FOUND`

**Acceptance Criteria:**
1. `IconBuilder::ERROR_END_MARKER_NOT_FOUND` exists with value `82305`.
2. The end-marker `strpos` failure branch in `insertIconCode()` uses `self::ERROR_END_MARKER_NOT_FOUND`.
3. The `build()` docblock includes a `@see` reference to `self::ERROR_END_MARKER_NOT_FOUND` for end-marker failures.
4. `test_build_phpEndMarkerMissing_returnsError` asserts `ERROR_END_MARKER_NOT_FOUND` (not `ERROR_START_MARKER_NOT_FOUND`).
5. `test_build_jsEndMarkerMissing_returnsError` exists and asserts `ERROR_END_MARKER_NOT_FOUND`.
6. All existing `IconBuilderTest` tests pass: `composer test-filter -- IconBuilder`.

**Estimated Complexity:** Low

**Notes:** Verify that error code `82305` is not already in use elsewhere in the codebase (grep for `82305`). If taken, use the next available code. This WP is fully independent — no dependencies on other WPs.

---

## WP-002 — Add `IconCollection::resetInstance()` for test isolation

**Description:** Add a `@internal` static `resetInstance()` method to `IconCollection` that nulls the singleton instance, enabling test isolation. Update the existing `IconCollectionTest` to call it in `tearDown()`.

**Scope:**
- `src/classes/UI/Icons/IconCollection.php` — new `resetInstance()` method
- `tests/AppFrameworkTests/UI/IconCollectionTest.php` — add `tearDown()` method

**Deliverables:**
- New `public static function resetInstance() : void` method on `IconCollection` with `@internal` annotation
- `IconCollectionTest` gains a `tearDown()` method that calls `IconCollection::resetInstance()`

**Acceptance Criteria:**
1. `IconCollection::resetInstance()` exists, is `public static`, and sets `self::$instance` to `null`.
2. The method has a `@internal` docblock annotation.
3. `IconCollectionTest::tearDown()` calls `IconCollection::resetInstance()`.
4. All existing `IconCollectionTest` tests pass: `composer test-filter -- IconCollection`.

**Estimated Complexity:** Low

**Notes:** This WP is fully independent — no dependencies on other WPs. The `@internal` annotation keeps the method out of the public API surface. Future test classes that manipulate singleton state can use this method in their own `tearDown()`.

---

## WP-003 — Consolidate `normaliseID()` duplication into `IconInfo`

**Description:** Extract the duplicated `normaliseID()` logic from `IconCollection` and `IconsReader` into a single public static method on `IconInfo`, and have both consumers delegate to it.

**Scope:**
- `src/classes/UI/Icons/IconInfo.php` — new `public static normaliseID()` method
- `src/classes/UI/Icons/IconCollection.php` — remove private `normaliseID()`, delegate to `IconInfo::normaliseID()`
- `src/classes/Application/Composer/IconBuilder/IconsReader.php` — remove private `normaliseID()`, add `use UI\Icons\IconInfo`, delegate to `IconInfo::normaliseID()`

**Deliverables:**
- New `public static function normaliseID(string $id) : string` on `IconInfo` that performs `str_replace(array('-', ' '), '_', $id)`
- `IconCollection` no longer has a private `normaliseID()` method; all call sites use `IconInfo::normaliseID()`
- `IconsReader` no longer has a private `normaliseID()` method; all call sites use `IconInfo::normaliseID()`; `use UI\Icons\IconInfo` import is added

**Acceptance Criteria:**
1. `IconInfo::normaliseID()` exists as a public static method and replaces hyphens and spaces with underscores.
2. `IconCollection` does not contain a private `normaliseID()` method.
3. `IconsReader` does not contain a private `normaliseID()` method.
4. `IconsReader` has a `use UI\Icons\IconInfo` import statement.
5. All `IconCollection` call sites for ID normalisation delegate to `IconInfo::normaliseID()`.
6. All `IconsReader` call sites for ID normalisation delegate to `IconInfo::normaliseID()`.
7. Existing tests pass: `composer test-filter -- IconBuilder` and `composer test-filter -- IconCollection`.

**Estimated Complexity:** Low

**Notes:** This WP should be completed before WP-004 (IconsReaderTest) and WP-005 (IconInfoTest), since those test files should exercise the consolidated method. The dependency direction (build-time `IconsReader` → runtime `IconInfo`) is acceptable: the build layer already produces artefacts consumed by the runtime layer.

---

## WP-004 — Create `IconsReaderTest.php`

**Description:** Add a dedicated unit test file for `IconsReader`, covering icon count, spinner exclusion, ID normalisation, sort order, missing-file handling, and `IconDefinition` property access.

**Scope:**
- `tests/AppFrameworkTests/Composer/IconBuilder/IconsReaderTest.php` (new file)

**Deliverables:**
- New test file `IconsReaderTest.php` in `tests/AppFrameworkTests/Composer/IconBuilder/`
- Six test methods covering all specified scenarios

**Acceptance Criteria:**
1. `IconsReaderTest.php` exists at `tests/AppFrameworkTests/Composer/IconBuilder/IconsReaderTest.php`.
2. Test `test_readsExpectedIconCount` verifies the icon count from the framework's `icons.json` (excluding spinners).
3. Test `test_excludesSpinnerIcons` verifies that spinner-type icons are absent from the definitions.
4. Test `test_normalisesIconIDs` verifies that hyphens/spaces in icon IDs are converted to underscores.
5. Test `test_definitionsAreSortedByID` verifies alphabetical sort order by ID.
6. Test `test_returnsEmptyOnMissingFile` verifies that a non-existent JSON path returns an empty list without throwing.
7. Test `test_iconDefinitionProperties` verifies `getID()`, `getIconName()`, and `getIconType()` on an `IconDefinition`.
8. All tests pass: `composer test-file -- tests/AppFrameworkTests/Composer/IconBuilder/IconsReaderTest.php`.

**Estimated Complexity:** Medium

**Notes:** Depends on WP-003 (normaliseID consolidation) being complete — the test for normalisation should exercise the consolidated path via `IconInfo::normaliseID()`. Use `ApplicationTestCase` as the base class. Follow patterns from `IconBuilderTest.php`. Run `composer dump-autoload` after creating the file (classmap autoloading).

---

## WP-005 — Create `IconInfoTest.php`

**Description:** Add a dedicated unit test file for `IconInfo`, covering getters, `getFullIconName()`, `createIcon()` factory, `getMethodName()` camelCase conversion, and the consolidated `normaliseID()` static method.

**Scope:**
- `tests/AppFrameworkTests/UI/IconInfoTest.php` (new file)

**Deliverables:**
- New test file `IconInfoTest.php` in `tests/AppFrameworkTests/UI/`
- Seven test methods covering all specified scenarios

**Acceptance Criteria:**
1. `IconInfoTest.php` exists at `tests/AppFrameworkTests/UI/IconInfoTest.php`.
2. Test `test_getID` verifies the ID getter returns the normalised icon ID.
3. Test `test_getPrefix` verifies the prefix getter returns the FA prefix string.
4. Test `test_getIconName` verifies the icon name getter returns the FA icon name.
5. Test `test_getFullIconName` verifies `prefix:name` format when prefix is present, and name-only when prefix is empty.
6. Test `test_createIcon` verifies the factory returns a `UI_Icon` instance with correct configuration.
7. Test `test_camelCaseConversion` verifies `getMethodName()` converts underscore-separated IDs to camelCase.
8. Test `test_normaliseID` verifies `IconInfo::normaliseID()` replaces hyphens and spaces with underscores.
9. All tests pass: `composer test-file -- tests/AppFrameworkTests/UI/IconInfoTest.php`.

**Estimated Complexity:** Medium

**Notes:** Depends on WP-003 (normaliseID consolidation) being complete — the `test_normaliseID` test covers the static method added in that WP. Use `ApplicationTestCase` as the base class. Follow patterns from `IconCollectionTest.php`. Run `composer dump-autoload` after creating the file.

---

## WP-006 — Create `AbstractLanguageRendererTest.php`

**Description:** Add a dedicated unit test file for `AbstractLanguageRenderer`, covering `toPascalCase()` conversion via a test subclass and `render()` output verification using a concrete renderer.

**Scope:**
- `tests/AppFrameworkTests/Composer/IconBuilder/AbstractLanguageRendererTest.php` (new file)

**Deliverables:**
- New test file `AbstractLanguageRendererTest.php` in `tests/AppFrameworkTests/Composer/IconBuilder/`
- A minimal concrete `TestableLanguageRenderer` subclass within the test file that exposes `toPascalCase()` as a public wrapper
- Three test methods covering all specified scenarios

**Acceptance Criteria:**
1. `AbstractLanguageRendererTest.php` exists at `tests/AppFrameworkTests/Composer/IconBuilder/AbstractLanguageRendererTest.php`.
2. A `TestableLanguageRenderer` class exists within the test file, extending `AbstractLanguageRenderer`, with a public `exposeToPascalCase()` method.
3. Test `test_toPascalCase` verifies underscore-separated IDs are converted to PascalCase (e.g., `time_tracker` → `TimeTracker`).
4. Test `test_renderProducesOutput` verifies that `render()` on a concrete renderer produces non-empty output when given icon definitions.
5. Test `test_renderContainsMethodNames` verifies the rendered output contains method names matching PascalCase-converted icon IDs.
6. All tests pass: `composer test-file -- tests/AppFrameworkTests/Composer/IconBuilder/AbstractLanguageRendererTest.php`.

**Estimated Complexity:** Low

**Notes:** This WP is independent of WP-003 (normaliseID consolidation). The `toPascalCase()` and `renderSetTypeArgs()` methods are `protected`, so the test subclass pattern is required. Follow patterns from `IconBuilderTest.php`. Run `composer dump-autoload` after creating the file.
