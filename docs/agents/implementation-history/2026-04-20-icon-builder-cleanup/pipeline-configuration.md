# Pipeline Configuration

> Generated by: Ledger Pipeline Configurator v1.0.0
> Source: `docs/agents/plans/2026-04-20-icon-builder-cleanup/work-packages-draft.md`

---

## Per-WP Stage Configuration

| WP | Title | active_pipeline_stages | Rationale |
|----|-------|------------------------|-----------|
| WP-001 | Add `ERROR_END_MARKER_NOT_FOUND` constant and fix end-marker error code | `["implementation", "qa", "code-review", "documentation"]` | Standard code change — adds a constant, fixes error code in production PHP, updates docblock, and modifies tests. No security surface (string marker validation only), no release artifacts. |
| WP-002 | Add `IconCollection::resetInstance()` for test isolation | `["implementation", "qa", "code-review", "documentation"]` | Standard code change — adds a new method to a production class and updates a test file. No security surface (`@internal` test-isolation method on a singleton), no release artifacts. |
| WP-003 | Consolidate `normaliseID()` duplication into `IconInfo` | `["implementation", "qa", "code-review", "documentation"]` | Standard code change — refactors existing logic across 3 production files (extract method, remove duplicates, delegate). No security surface (string normalization: hyphens/spaces → underscores), no release artifacts. |
| WP-004 | Create `IconsReaderTest.php` | `["qa", "code-review"]` | **Verification-only** — creates a new test file with no production code or documentation changes. All tested symbols (`IconsReader`, `IconDefinition`, `IconInfo::normaliseID()`) will exist in production by execution time (WP-003 dependency ensures `normaliseID()` is consolidated before this WP runs). |
| WP-005 | Create `IconInfoTest.php` | `["qa", "code-review"]` | **Verification-only** — creates a new test file with no production code or documentation changes. All tested symbols (`IconInfo` getters, `getMethodName()`, `createIcon()`, `normaliseID()`) will exist in production by execution time (WP-003 dependency ensures `normaliseID()` is consolidated before this WP runs). |
| WP-006 | Create `AbstractLanguageRendererTest.php` | `["qa", "code-review"]` | **Verification-only** — creates a new test file with no production code or documentation changes. All tested symbols (`AbstractLanguageRenderer::toPascalCase()`, `render()`, concrete renderers) already exist in production code. No dependencies on other WPs. |

## Guardrail Notes

1. **No security-audit required for any WP.** None of the 6 WPs touch authentication, authorization, session handling, sensitive user data, external APIs, cryptography, secrets, file uploads, user-supplied paths, SQL queries, or access control logic. The work is limited to error constants, singleton reset methods, string normalization refactoring, and new unit test files.

2. **No release-engineering required for any WP.** None of the 6 WPs produce publishable artifacts, introduce breaking API changes, require version bumps, or involve migration steps. All changes are internal to the Application Framework repository with no downstream consumer impact.

3. **WP-004 and WP-005 use verification-only chain `["qa", "code-review"]` instead of the standard 4-stage default.** These WPs create new test files only — no production code or documentation is modified. The `implementation` stage is omitted because the Developer agent writes the test file during the `qa` stage (test authoring is QA work). The `documentation` stage is omitted because there are no docs, READMEs, or manifests to update. Both WPs depend on WP-003 completing first, which guarantees that `IconInfo::normaliseID()` exists in production before tests are written against it.

4. **WP-006 uses verification-only chain `["qa", "code-review"]` for the same reasons as WP-004/005.** It has no dependencies — all symbols under test (`AbstractLanguageRenderer`, `toPascalCase()`, `render()`, `PHPRenderer`/`JSRenderer`) already exist in production code.

5. **No ordering constraint violations.** All configured stage lists are valid subsequences of the canonical ordering: `implementation → qa → security-audit → code-review → release-engineering → documentation`.
