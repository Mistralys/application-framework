# Dependency & Sequencing Analysis

> Generated by: Ledger Dependency Sequencer v1.0.1
> Source: `docs/agents/plans/2026-04-20-icon-builder-cleanup/work-packages-draft.md`

---

## Dependency Graph

| WP | Title | Dependencies | Justification |
|----|-------|-------------|---------------|
| WP-001 | Add `ERROR_END_MARKER_NOT_FOUND` constant and fix end-marker error code | none | Modifies `IconBuilder.php` and `IconBuilderTest.php` — no shared artifacts with other WPs |
| WP-002 | Add `IconCollection::resetInstance()` for test isolation | none | Adds a new method to `IconCollection.php` and updates `IconCollectionTest.php` — no functional overlap with other WPs |
| WP-003 | Consolidate `normaliseID()` duplication into `IconInfo` | none | Modifies `IconInfo.php`, `IconCollection.php`, and `IconsReader.php` — introduces `IconInfo::normaliseID()` that WP-004 and WP-005 depend on |
| WP-004 | Create `IconsReaderTest.php` | WP-003 | Tests `IconsReader` including normalised ID behavior; the test must exercise `IconInfo::normaliseID()` delegation introduced by WP-003 |
| WP-005 | Create `IconInfoTest.php` | WP-003 | Tests `IconInfo` including `IconInfo::normaliseID()` static method introduced by WP-003 |
| WP-006 | Create `AbstractLanguageRendererTest.php` | none | Creates a new test file for `AbstractLanguageRenderer`; no shared artifacts or ordering constraints with any other WP |

## Execution Phases

### Phase 1 (Parallel)
- **WP-001:** Add `ERROR_END_MARKER_NOT_FOUND` constant and fix end-marker error code
- **WP-002:** Add `IconCollection::resetInstance()` for test isolation
- **WP-003:** Consolidate `normaliseID()` duplication into `IconInfo`
- **WP-006:** Create `AbstractLanguageRendererTest.php`

### Phase 2 (Parallel within phase)
- **WP-004:** Create `IconsReaderTest.php` (depends on WP-003)
- **WP-005:** Create `IconInfoTest.php` (depends on WP-003)

## Parallelization Notes

### Phase 1

- **WP-001** and **WP-006** are fully independent — no shared files or artifacts. Safe to run in parallel.
- **WP-001** and **WP-002** are fully independent — different source files, different test files. Safe to run in parallel.
- **WP-001** and **WP-003** are fully independent — WP-001 touches `IconBuilder.php`/`IconBuilderTest.php`, WP-003 touches `IconInfo.php`/`IconCollection.php`/`IconsReader.php`. Safe to run in parallel.
- **WP-002** and **WP-003** share a file: both modify `IconCollection.php`. However, WP-002 adds a new `resetInstance()` method while WP-003 removes the private `normaliseID()` method and updates its call sites. These changes affect different sections of the file with no functional overlap. **Parallelizable with caution** — if implemented as concurrent code changes, merge conflicts in `IconCollection.php` are possible but trivially resolvable (non-overlapping hunks). Sequential execution within Phase 1 eliminates this risk entirely.
- **WP-006** is fully independent of all other Phase 1 WPs — it creates a new test file and modifies no existing source files.

### Phase 2

- **WP-004** and **WP-005** are fully independent of each other — they create different new test files (`IconsReaderTest.php` vs `IconInfoTest.php`) in different directories with no shared artifacts. Safe to run in parallel.
- Both WP-004 and WP-005 depend only on WP-003 being complete (they test the consolidated `IconInfo::normaliseID()` method).

## Critical Path

The longest sequential chain is:

```
WP-003 → WP-004  (2 sequential stages)
WP-003 → WP-005  (2 sequential stages)
```

Both chains are of equal length (2 stages). The minimum elapsed time is **2 phases**.

WP-001, WP-002, and WP-006 are all on independent single-stage paths (Phase 1 only) and do not contribute to the critical path.

### Summary

```
Phase 1:  WP-001 ─────────────────────────────
          WP-002 ─────────────────────────────
          WP-003 ─────────────────────────────  ← critical path start
          WP-006 ─────────────────────────────

Phase 2:                WP-004 ───────────────  ← critical path end (parallel)
                        WP-005 ───────────────  ← critical path end (parallel)
```
